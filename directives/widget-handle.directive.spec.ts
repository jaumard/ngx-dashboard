/* tslint:disable:no-unused-variable */

import {WidgetHandleDirective} from "./widget-handle.directive";

describe('Directive: WidgetHandle', () => {
  it('should create an instance', () => {
    let directive = new WidgetHandleDirective();
    expect(directive).toBeTruthy();
  });
});
